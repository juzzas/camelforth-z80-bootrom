HEX

: TFAIL  ( dsk blk buffer -- f )
   .S ." T-FAIL" CR  DROP DROP DROP  0  ;

: TOK   ( dsk blk buffer -- f )
   .S ." T-OK" CR   DROP DROP DROP  -1  ;

' TOK BLKREADVEC !
' TOK BLKWRITEVEC !

8001 DSK !

BLOCK-READ
BLOCK-WRITE

' TFAIL BLKREADVEC !
' TFAIL BLKWRITEVEC !

BLOCK-READ
BLOCK-WRITE

0 BLKREADVEC !
0 BLKWRITEVEC !

BLOCK-READ
BLOCK-WRITE
